[project]
name = "bot-commander"
version = "0.1.0"
description = "Reusable bot command framework with Telegram and XMPP adapters"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
telegram = ["telegram-bot"]
xmpp = ["xmpp-bot", "aiodns<4.0.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/bot_commander"]

[tool.uv.sources]
telegram-bot = { git = "https://github.com/BenjaminKobjolke/telegram-bot.git" }
xmpp-bot = { git = "https://github.com/BenjaminKobjolke/xmpp-bot.git" }

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "ruff>=0.4.0",
]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "W", "UP", "B", "SIM"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
